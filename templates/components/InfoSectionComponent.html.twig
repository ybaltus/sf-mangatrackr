<div{{ attributes }}>
    <!-- component html -->
    {% if not inline %}
        <section class="bg-app-clear-white dark:bg-app-dark-black">
            <div class="py-8 px-4 mx-auto max-w-screen-xl text-center lg:py-16 lg:px-12">
                <h2 class="mb-4 text-3xl lg:text-4xl tracking-tight font-extrabold text-app-clear-black dark:text-app-dark-gray2">
                    {{ title }}
                </h2>
                <p class="mb-8 text-lg font-normal text-gray-500 lg:text-xl sm:px-16 xl:px-48 dark:text-gray-400">
                    {{ info }}
                </p>
                <div class="px-4 mx-auto text-center md:max-w-screen-md lg:max-w-screen-lg lg:px-36">
                    {{ component('ButtonGradientComponent', {
                        title: 'Découvrir'
                    }) }}
                </div>
            </div>
        </section>
    {% else %}
        <section class="bg-app-clear-white dark:bg-app-dark-black">
            <div class="grid max-w-screen-xl px-4 py-8 mx-auto lg:gap-8 xl:gap-0 lg:py-16 lg:grid-cols-12">
                <div class="mr-auto place-self-center lg:col-span-7">
                    <p class="max-w-2xl mb-6 font-normal text-app-clear-black lg:mb-8 md:text-lg lg:text-xl dark:text-app-dark-gray2">
                        {{ info }}
                    </p>
                </div>
                <div class="lg:mt-0 lg:col-span-5 lg:flex">
                    {{ component('ButtonGradientComponent', {
                        title: 'S\'inscrire'
                    }) }}
                </div>
            </div>
        </section>
    {% endif %}
</div>
