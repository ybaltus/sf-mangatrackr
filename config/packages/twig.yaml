twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        app_name: '%env(APP_NAME)%'
        app_mail_generic: '%env(APP_MAIL_GENERIC)%'
        app_team_name_1: '%env(APP_TEAM_NAME_1)%'
        app_team_name_2: '%env(APP_TEAM_NAME_2)%'

when@test:
    twig:
        strict_variables: true
